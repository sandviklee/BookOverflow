image: node:18.14.0

stages:
  - build
  - test

cache:
  paths:
    - node_modules/

build site:
  stage: build
  script:
    - npm ci --progress=false
    - npm run build

# unit test:
#   stage: test
#   script:
#     - npm test
#   artifacts:
#     paths:
#       - dist/
#       - node_modules/
#       - package.json